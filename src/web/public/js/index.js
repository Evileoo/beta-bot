import mysql from "../../../node_modules/mysql2-async";

window.onload = async () => {

    console.log(mysql);

    //const code = window.location.search.split("=")[1];
    //const data = await db.query(`SELECT * FROM api_access WHERE code = '${code}'`);
    //console.log(data[0]);
}